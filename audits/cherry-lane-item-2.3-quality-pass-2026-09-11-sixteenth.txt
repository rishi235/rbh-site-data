Item 2.3 (Cherry Lane Pharmacy, Walton) - sixteenth quality pass
Run: 2026-09-11, unattended scheduled run (audit-backlog-worker), Cowork Linux
sandbox shell for repo work; mcp__Windows-MCP__PowerShell used for git add/
commit/push after bash's own git status left an unlinkable .git\index.lock
(same standing constraint logged on every run since 2026-09-05, Q96).

REPO HALF
Full repo exported clean via `git archive HEAD` into a scratch directory
(/tmp/scratch23), tracked repo never opened for writing during this pass.
All six generators (build-service-pages.js, build-contraception-pages.js,
build-switch-pages.js, build-travel-clinic-pages.js, build-weight-loss-
pages.js, build-branch-landing-pages.js) rebuilt to a byte-identical tree
(diff -rq against the tracked repo's modules/ showed no difference other
than a pre-existing, unrelated stray file left in the tracked working
directory by an earlier session:
modules/service/pages/notarealservice-fishlocks-ainsdale.html.bak - not
part of this item, not touched, filtered out of check-page-coverage.js's
own scan because it does not end .html).

Baseline: all 35 checkers run against the scratch copy exit 0
(check-cdn-pins.js excluded, needs live jsDelivr resolution, same
convention as every prior pass).

FRESH ANGLE: tools/check-page-coverage.js had never been proven by
injection against Cherry Lane's own branch record or generated page set in
fifteen prior passes (confirmed absent from this item's own text by a
whitespace-collapsed grep across all fifteen prior write-ups before this
run started). This is the checker that answers whether the SET of pages
matches what branches.json earns - the single most direct test of this
item's own core claim ("full 12-page set exists"), so it is a natural gap
to have missed for this long.

Four injections on the scratch copy, each restored (byte copy or, for the
deleted page, regeneration from the untouched generator) and SHA256-
reconfirmed against a pre-injection baseline hash before the next
injection and after the last:

1. branches.json: removed widgets.contraception from cherrylane_liverpool.
   Caught: 2 failures - NOT_EARNED (Contraception: build-contraception-
   pages.js still lists cherrylane_liverpool but branches.json no longer
   earns it a page) plus the correctly cascading ORPHAN_PAGE (the existing
   contraception-cherry-lane-walton.html file is now unearned). Restored;
   branches.json SHA256 904de09b...9046811b confirmed unchanged.

2. tools/build-switch-pages.js: removed the cherrylane_liverpool entry
   from the EXTRAS object (the switch generator's driving list since Q19).
   Caught: NOT_BUILT (Switch: branch cherrylane_liverpool earns a page,
   sluggable, but is no longer in the EXTRAS object of
   tools/build-switch-pages.js). Restored; SHA256
   abb6cdb0...badc9046d8 confirmed unchanged.

3. modules/service/pages/weight-loss-clinic-cherry-lane-walton.html:
   deleted, while branches.json still sets widgets.weightLoss and the
   generator's BUILD list still lists the branch. Caught: PAGE_MISSING
   (Weight loss: earned by cherrylane_liverpool but not on disk). Restored
   by re-running the untouched build-weight-loss-pages.js generator;
   SHA256 60f65541...aad4df90d confirmed byte-identical to the
   pre-injection baseline, and a full diff of modules/service/pages against
   the tracked repo showed no other file changed by the regeneration.

4. modules/service/pages/: added an orphan file, a byte-copy of
   contraception-cherry-lane-walton.html renamed to
   contraception-cherry-lane-walton-old.html. Caught: ORPHAN_PAGE (on disk
   but branches.json earns no such page). Removed after.

All four caught on their intended rule, first attempt. Final restore:
full 35-checker suite (check-cdn-pins.js excluded) re-run clean, all four
touched files SHA256-reconfirmed byte-identical to their pre-injection
baselines, tracked repo (/sessions/.../mnt/rbh-site-data) confirmed
untouched throughout via `git status --porcelain modules/ branches.json
tools/` (no output for those paths before or after). No checker gap
found. No in-repo defect. No rule or page byte changed.

LIVE HALF (Claude in Chrome MCP not connected this run - "Claude in Chrome
is not connected" on tabs_context_mcp - standing Q59, not retried by any
other route, no login attempted; fell back to a read-only curl GET from
the Cowork sandbox shell against two live pages, no click, no submit, no
form, no login).

1. pharmacy-first-cherry-lane-walton.html (200). SIGNIFICANT FINDING: Q95's
   fault is BACK live. Five of the seven NHS Pharmacy First condition cards
   (matched by the "Page coming soon" marker in server-rendered HTML, byte-
   level, not client-injected) show no working link, sitting inside an
   <em> tag rather than an anchor: sinusitis, earache, impetigo, shingles,
   and infected insect bite. Sore throat and UTI still render as real
   anchors (href="/sore-throat-treatment-cherry-lane-walton.html" and
   href="/uti-treatment-cherry-lane-walton.html" both present). This is
   the same five-card pattern seen broken on the tenth and eleventh passes
   (2026-09-04, 2026-09-05), seen fixed on the eighth and fifteenth passes
   (2026-09-02, 2026-09-09), and not tested on several passes in between.
   The repo's own generated page remains correct throughout (all seven as
   working links; not modified this run). A note with this evidence was
   appended to Q95 (status left open, not closed and not re-raised as a
   new question - it is the same recurring fault, now confirmed unstable
   for a fourth time rather than durable).

2. switch-prescriptions-cherry-lane-walton.html (200). Two previously
   logged live-only faults, neither in copy this repo owns, both
   reconfirmed unchanged: the <title> tag still reads the pre-Phase-3
   string "Switch Your Prescriptions - Cherry Lane Pharmacy Walton"
   against the paste sheet's correct "Switch Your Prescriptions to Cherry
   Lane Pharmacy, Walton" (tracked under 3.1/5.1/Q3, not re-raised); and
   the "How switching to Cherry Lane Pharmacy works" lead paragraph still
   renders the em dash as visible mojibake, read via curl+Python as
   "" (Windows-1252 misdecoding of the correct UTF-8 em dash bytes E2 80
   94 - consistent with the raw byte value C3 94 C3 87 C3 B6 recorded on
   the eleventh pass and the DOM character codes U+00D4 U+00C7 U+00F6
   recorded on the fifteenth pass; three different read methods across
   three passes agree on the same corrupted bytes). The Q36 NHS mailbox
   footer typo was NOT observed this pass - a full email-address sweep of
   the switch page's raw HTML found only Cherry@rbhealth.co.uk, no NHS
   mailbox string at all - consistent with the fault's already-logged
   "unstable between deploys or caches" pattern (seen absent on the
   2026-08-11 second pass and the seventh/ninth/thirteenth/fourteenth
   passes, seen present as the typo on the 2026-08-13/2026-08-30/eighth/
   fifteenth passes). Not re-raised.

No new question raised beyond the Q95 evidence note. QUESTIONS.json total
count unchanged (101 entries; still 48 open after this note, since Q95 was
already open).

Rotation-pool computation for the next unattended run: today's touched
items (by `git log --since="2026-09-11T00:00:00" --format="%aI %s" --
AGENT_WORKLIST.md`) are 3.1, 4.3, 4.6, 4.5, 1.3, 4.13, 4.8, 4.10, 4.9,
4.12, 4.15, and now 2.3. Per the fifteenth pass's own forward note, the
next stalest untouched item today is 3.3 (last touched
2026-09-09T15:06:15+01:00).
