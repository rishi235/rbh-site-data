Item 4.7 quality pass (seventeenth), 2026-09-11
McCanns Chemist Sandringham GBP pack (gbp-packs/mccanns-sandringham.md)
Unattended scheduled run, audit-backlog-worker, Cowork sandbox (mcp__workspace__bash)
throughout; Claude in Chrome MCP was not connected this session (navigate to
https://data.rbhealth.co.uk/api/feedback returned "Claude in Chrome is not connected"),
not retried by any other route, no login attempted; plain curl HTTP GET/HEAD (bash
tool, outbound network reachable this run) was used for the live half instead.

SELECTION
All 8 unchecked AGENT_WORKLIST.md lines confirmed [BLOCKED] by direct grep (5.3, 5.4,
5.5, 5.8, 6.1, and the three Q60/Q66 lines under 6.4/6.5/6.6), so the quality-pass
fallback applied. Rotation pick re-derived independently and from scratch, not taken
from any prior run's forward note: enumerated the 43 checked worklist items by
grepping "^- \[x\]" headers (all item numbers plus checked/unchecked/blocked status,
using item-start boundaries for every line so blocked items' own text could not
contaminate an adjacent checked item's block), subtracted the seven standing
out-of-rotation one-offs (1.1, 1.4, 2.2, 5.6, 5.7, 6.7, 6.8 - confirmed against the
explicit correction recorded in item 5.7's own 2026-08-30 quality-pass entry) to get
a 36-item pool, then subtracted today's fourteen already-touched items (4.2, 3.3, 2.3,
4.15, 4.12, 4.9, 4.10, 4.8, 4.13, 1.3, 4.5, 4.6, 4.3, 3.1, via
`git log --since="2026-09-11T00:00:00" --format="%aI %s" -- AGENT_WORKLIST.md`). For
each of the 22 remaining candidates, found the true most recent touch via
`git log --all --format="%aI %s" -- AGENT_WORKLIST.md | grep -E "Item N\.N[^0-9.]"`
(matching the item number followed by a non-digit, non-dot character, to avoid 4.1
matching inside 4.11 etc). Stalest nine were all last touched 2026-09-09: 4.7
(15:45:04), 4.14 (16:12:43), 1.2 (18:46:19), 3.11 (19:14:46), 4.4 (19:43:33), 4.1
(20:13:24), 3.2 (22:44:22), 3.5 (22:53:24); everything else in the pool was touched
2026-09-10 or later. 4.7 was uniquely stalest at 2026-09-09T15:45:04+01:00 (its own
sixteenth pass). Chosen: 4.7 (McCanns Chemist Sandringham GBP pack).

Note on method: an initial pass at this used a cruder "latest date-shaped string
anywhere in the item's text block" heuristic, which mis-ranked 3.7 (a stray
"2026-09-09" substring earlier in its block outranked its real, later 2026-09-10
touch once block boundaries were fixed) - discarded in favour of the git-log method
above, which reads actual commit timestamps rather than prose.

BASELINE
Pack sha256 bc9ab580dd9a5532a3fec55d05c599f9d5fc98f3a3bb9ba9f5882bd65e1a893e, matching
every one of the sixteen prior passes exactly - no drift. Full repo exported via
`git archive HEAD | tar -x` to /tmp/scratch47, a scratch copy entirely outside the
tracked working tree; the tracked repo was never opened for writing at any point in
this pass. Full 34-checker suite (check-cdn-pins.js and check-live-hours.js excluded,
standing convention - both need a live fetch) run clean on the scratch copy, 0
nonzero exits, before any edit. All three injections below were made on the scratch
copy only, each restored by byte copy (cp) from a saved baseline and reconfirmed
identical by both sha256 and cmp before the next injection - the pack was never left
in a modified state between rounds, and the tracked repo's own copy was never touched.

FRESH ANGLE
Sixteen prior passes had proven fourteen separate rule families in check-gbp-packs.js,
check-gbp-pharmacy-first.js, check-pharmacy-first-eligibility.js,
check-pharmacy-first-cost.js, check-postcodes.js, check-uk-spelling.js,
check-brand-spelling.js, check-url-scheme.js, check-app-membership.js and
check-branch-links.js by injection against this pack's own copy - but the photo shot
list rules in check-gbp-packs.js (photoCount, photoVinyl, photoGoogleUpdates, added on
the item 4.3 quality pass, 2026-08-11) had never been named once across sixteen
passes' worth of log text (confirmed by grep, zero hits for "photo" anywhere in item
4.7's history). This is notable because the checker's own comment records that
mccanns-sandringham.md was the pack the rule found missing the pending-Google-updates
reminder when the rule was first written - the rule's own origin story runs through
this exact pack, and it had still never been proven by injection against it in its
own item's rotation.

Three injections on the scratch copy only, each restored by byte copy and sha256/cmp-
reconfirmed identical to baseline before the next:

(1) photoCount - removed the tenth (last) photo bullet, "Street view along Aigburth
Road showing the shopfront in context, so customers can recognise the branch on
approach.", dropping the section to 9 shots - CAUGHT, first attempt: "Photo shot list
names 9 shots, and Build Pack v2 4.1 asks for at least 10."

(2) photoVinyl - restored baseline, then collapsed the vinyl bullet ("Close-up of the
storefront signage; if the new vinyl has been fitted at this branch, make it the lead
exterior shot (Build Pack 4.1 asks for the vinyl storefront in the photo set).") down
to a single plain bullet with no mention of vinyl, keeping the bullet count at exactly
10 so photoCount could not also fire - CAUGHT, first attempt, on its own rule only:
"Photo shot list does not mention the vinyl storefront". No cross-firing with
photoCount confirmed by reading the full failure output for this pack, one FAIL line
only.

(3) photoGoogleUpdates - restored baseline, then removed the sentence "While in the
profile, also action any pending Google updates: Google's own suggested edits to
hours, categories and the address publish themselves if nobody clears them." from the
end of section 4 only, leaving the separate, near-identical reminder in the "Notes for
the paster" section (line 172-174 of the pack) untouched - CAUGHT, first attempt:
"Photo shot list does not remind the paster to action any pending Google updates".
This also positively confirms the rule is correctly SCOPED to section 4
(photoSecRaw) and does not accept the same reminder appearing elsewhere in the file
as satisfying it - a pack could otherwise carry the sentence once in its paster notes
and never in the photo section itself, and the rule would still need to catch that,
which it does.

All three injections fired first attempt, each on its own intended rule, with no
cross-firing observed in either direction. Full 34-checker suite re-run clean on the
scratch copy after the final restore (0 nonzero exits); pack sha256 and cmp
reconfirmed identical to baseline after each restore and again at the end. All six
page generators re-run against the tracked repo, zero-diff worktree
(`git status --porcelain -- modules core` shows nothing new). `git status --porcelain
-- gbp-packs tools modules core branches.json` against the TRACKED repo shows only
two pre-existing, out-of-scope untracked strays unrelated to this pass
(modules/service/pages/notarealservice-fishlocks-ainsdale.html.bak, documented in
earlier passes, and a transient gbp-packs/.fuse_hidden* mount artefact that
disappeared on its own) - no tracked file was touched.

RESULT: zero in-repo defect. All three photo shot list rules already correctly
protect this pack, now proven directly by injection against its own copy for the
first time in seventeen passes, rather than only by the passive "photo shot list
present and complete" observation recorded when the rule was first written.

LIVE HALF
Claude in Chrome not connected (Q59, unchanged); not retried by any other route, no
login attempted, per procedure. Plain curl HTTP GET/HEAD used instead (outbound
network reachable this run):
- pharmacy-mccanns-sandringham.html: still 404, still absent from sitemap.xml (Q35,
  unchanged since every prior pass).
- The four post-target pages (pharmacy-first, switch, weight-loss-clinic,
  travel-clinic, all -mccanns-sandringham.html) all still 200.
- sitemap.xml lastmod for this branch's URLs still 2026-08-14T23:05:25+00:00,
  unchanged, confirming no republish since the sixteenth pass's own reading.
No new question; all five live findings reconfirmed unchanged, none of them touched
by this pass's fresh angle (which was pack-copy-only, not live-site-facing).

QUESTIONS.json total unchanged at 101 entries; no question added or answered this
pass (answer pickup unavailable per Q59; nothing in this pass's own findings met the
bar for a new question).

FILES CHANGED THIS PASS: audits/mccanns-sandringham-item-4.7-quality-pass-2026-09-11-
seventeenth.txt (new), AGENT_WORKLIST.md (item 4.7 paragraph appended), AGENT_LOG.md
(this run's entry). No changes to gbp-packs/, modules/, core/, tools/ or
branches.json.
