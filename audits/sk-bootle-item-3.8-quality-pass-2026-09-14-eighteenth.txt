Item 3.8 quality pass (eighteenth), 2026-09-14
Branch: skchemists_bootle (SK Chemists, Stanley Road, Bootle, odsCode FH575)
Fresh angle: tools/check-branch-identity.js, never named once across seventeen
prior passes on this item, despite covering fourteen other checkers on this
branch.

WHY THIS CHECKER: SK Chemists Bootle sits 1.5 miles from Smartts Chemist
Bootle in the same town (the CLAUDE.md-named highest-risk adjacent-branch
pair for copy-paste faults - see the hasApp section). check-branch-identity.js
guards the two machine-readable fields (data-branch, JSON-LD name) that no
visible-text checker reads, plus the branches.json-level brandLabel/branchName
relationship (rule 7) and outbound review links (rule 8). SK Bootle is a
"simple" case for this checker - brandLabel equals branchName ("SK Chemists"),
and it is not one of the three shared-domain brand pairs the checker's own
AMBIGUOUS rule (4) exists for - so this pass proves the checker's baseline
rules hold on a standalone-brand branch, complementing the shared-brand pairs
(Fishlocks, McCanns, Scorah) already proven on earlier items.

BASELINE: branches.json sha256 904de09bc3118cefcfd7ae3f8e045b9ea1d090c634c701
14f135101f0b969e1e, matching the standing regression anchor. Full 36-checker
suite clean on the tracked repo before any work. Full-repo scratch copy via
`git archive HEAD | tar -x`; five target files (branches.json, tools/check-
branch-identity.js, modules/service/pages/pharmacy-first-sk-chemists-
bootle.html, contraception-sk-chemists-bootle.html, travel-clinic-sk-
chemists-bootle.html) sha256-confirmed matching the tracked repo. 34/34
checkers clean on the scratch copy (check-cdn-pins.js and check-live-hours.js
excluded from scratch runs by established convention - the former fails on
any .git-less copy by design, the latter makes live HTTP calls). check-
branch-identity.js itself confirmed clean on the scratch copy before any
injection: 177 pages matched, 198 review links, 6 sister-link labels read.

Note: SK Chemists Bootle has no branch landing page (it is not one of the
six branches on a shared domain that build-branch-landing-pages.js covers)
and no sister branch, so rules 5 (SITEUNIQUE, requires two branches sharing
a host) and 9 (SISTERLINK, requires a landing-page link to a sister) have no
natural injection point on this branch and were not exercised. This is a
genuine scope limit of the branch, not a gap left untested by choice.

FIVE INJECTIONS, each restored by direct copy from the tracked repo and
sha256-reconfirmed byte-identical before the next:

1. RULE 2 (OWNER): pharmacy-first-sk-chemists-bootle.html's data-branch
   changed from "SK Chemists" to "Riddings Pharmacy" (another live branch's
   name). CAUGHT - 2 failures: the page-level OWNER mismatch naming both
   branches by name, plus the expected collateral RULE 6 (SPLIT) firing
   because the branch's own pages now disagree with each other (11 pages say
   "SK Chemists", 1 says "Riddings Pharmacy").

2. RULE 3 (SCHEMANAME): contraception-sk-chemists-bootle.html's JSON-LD
   "name" changed from "SK Chemists" to "Riddings Pharmacy". CAUGHT - 2
   failures: the page-level SCHEMANAME mismatch, plus the same RULE 6
   collateral as injection 1.

3. RULE 7 (DATA, branches.json level): skchemists_bootle's branchName
   changed from "SK Chemists" to "Bootle Chemists" (no longer starts with
   brandLabel "SK Chemists"). CAUGHT - exactly 1 failure, no collateral
   (this rule reads branches.json directly, independent of any page).

4. RULE 8 (OUTBOUND): pharmacy-first-sk-chemists-bootle.html's Google review
   link swapped from SK Bootle's own (https://g.page/r/CeAIYeqSjIYBEAE/review)
   to riddings_timperley's real googleReviewUrl. CAUGHT - exactly 1 failure,
   naming both branches and the specific risk (a patient review would land
   on the wrong shop's public profile, unrecoverable by a repaste).

5. CONTROL: an unrelated field (the printed phone number, 0151 944 1013, on
   the same pharmacy-first page) changed to a clearly wrong value with no
   identity field touched. Correctly PASSED - 0 failures, confirming the
   checker does not cross-fire on unrelated content it is not built to read.

All five injections caught (or passed) on exactly their intended rule(s),
first attempt, no unexplained cross-firing. Final restore sha256-reconfirmed
identical to baseline for all three files touched. Full 34-checker suite
(excluding check-cdn-pins.js, check-live-hours.js) re-run clean on the
scratch copy after all injections and restores.

Tracked repo confirmed untouched throughout: `git status --porcelain --
modules core branches.json gbp-packs tools` showed only the two pre-existing
untracked strays (gbp-packs/.fuse_hidden0000000400000001, modules/service/
pages/notarealservice-fishlocks-ainsdale.html.bak), unchanged. Full 36-
checker suite re-run individually against the tracked repo after cleanup:
36/36 exit 0.

RESULT: zero in-repo defect found. check-branch-identity.js is already
correctly protecting SK Chemists Bootle's own pages on rules 2, 3, 6, 7 and
8, proven for the first time this pass. No page, generator or branches.json
content changed.

LIVE HALF: not attempted. mcp__claude-in-chrome__tabs_context_mcp called
once, reported "Claude in Chrome is not connected" (standing Q59 procedure -
no retry, no alternative route, no login attempted). This checker's live
surface (data-branch and JSON-LD are both invisible to a person reading the
page) offers limited value from a visual spot-check in any case; no live
escalation attempted this pass.

Eighteen checkers now proven for item 3.8 across eighteen passes:
check-app-membership.js, check-booking-routes.js, check-branch-identity.js
(this pass), check-branch-links.js, check-cdn-pins.js, check-contraception-
copy.js, check-gbp-packs.js, check-jsonld.js, check-map-embeds.js, check-
nap.js, check-postcodes.js, check-seo-keywords.js, check-seo-pattern.js,
check-switch-copy.js, check-weight-loss-copy.js, check-whatsapp-route.js.

Checkers still never named against SK Chemists Bootle specifically (a
candidate list for a nineteenth pass): check-address-region.js, check-
brand-spelling.js, check-editor-snapshot.js, check-em-dashes.js, check-
fragment-targets.js, check-gbp-pharmacy-first.js, check-live-hours.js,
check-opening-hours.js (limited value - SK Bootle has no branch landing page,
so only rules 8/9 of that checker have a natural injection point here),
check-page-coverage.js, check-pharmacy-first-cost.js, check-pharmacy-first-
eligibility.js, check-pharmacy-first-safety-net.js, check-pharmacy-first-
symptoms.js, check-seo-lengths.js, check-seo-sheets.js, check-service-
links.js, check-travel-clinic-copy.js, check-uk-spelling.js, check-url-
scheme.js, check-widget-diaries.js.
