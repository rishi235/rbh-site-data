Item 4.13 (Riddings Pharmacy Timperley GBP pack) - thirteenth quality pass
2026-09-06, unattended scheduled run (audit-backlog-worker)

SELECTION
All 8 unchecked AGENT_WORKLIST.md items confirmed [BLOCKED] by direct grep
(5.3 Q8, 5.4 Q9, 5.5 Q13, 5.8 Q16, 6.1 Q52, 6.4 Q60, 6.5 Q60, 6.6 Q66), so the
quality-pass fallback applied. Rotation pool: 43 checked items minus the six
standing out-of-rotation items (1.1, 1.4, 5.6, 5.7, 6.7, 6.8) = 37-item pool.
Per-item last-touched date taken via
`git log -1 --format=%cI --no-patch -L<start>,<end>:AGENT_WORKLIST.md` over
each item's own paragraph range. Item 1.3 had just been re-verified earlier
today (2026-09-06, fourteenth pass on that item), so it dropped out of
contention; item 4.13 came out stalest of the remaining pool at
2026-09-05T00:12:41+01:00 (its twelfth-pass timestamp), ahead of every other
item in the pool.

BASELINE
git status --porcelain on gbp-packs/, modules/, core/, tools/, branches.json,
status/: empty. sha256 of gbp-packs/riddings-timperley.md:
8cc587968d3f6b83a3509aa27151c7dc30172b626b9d0fed824630a775917c04, unchanged
since the tenth pass (2026-09-03). All 36 tools/check-*.js run individually
on the tracked repo: 36/36 exit 0.

NEW ANGLE
The category/service ALLOWLIST rule in tools/check-gbp-packs.js (added item
4.4 quality pass, 2026-08-13, proved at birth against gbp-packs/
scorah-bramhall.md: an invented service "Ear wax removal" and an invented
category "Dental clinic" both walked past all 30 checkers of that day) had
never been individually injection-tested against gbp-packs/
riddings-timperley.md across this item's twelve prior passes. The BLOCKLIST
half of the same code region (CATEGORY_RULES/SERVICE_RULES, the "branch
doesn't have this widget" direction) could not be meaningfully injection
tested on this pack either, at birth or since, because Riddings holds all
five widgets (pharmacyFirst, bloodPressure, contraception, weightLoss,
travelClinic per branches.json), so every category and service the checker
knows about is already earned; there is no "not offered" case to trigger.
The ALLOWLIST rule (RECOGNISED_SERVICES / RECOGNISED_CATEGORIES) is the
INVENTED-CLAIM direction (a label the checker has never heard of at all,
regardless of widgets) and was the genuinely untested surface here.

METHOD
Full repo copied to a scratch directory outside the tracked working tree
(/tmp/rbh-scratch-4.13-13th), excluding only .git, per the lesson recorded on
the item 4.13 twelfth pass (a partial copy of gbp-packs/+tools/+branches.json
alone produces false failures because check-gbp-packs.js cross-references
modules/*/pages/*.html for link targets). Scratch baseline re-confirmed
before any injection: sha256 of riddings-timperley.md identical to the
tracked file, node tools/check-gbp-packs.js exits 0 with the same 17
estate-wide WARNs as every recent pass (Q72 qualifier-exemption x3, Q64
address-locality x4, three live-only link-target warnings, one phone-like
number warning, plus Riddings' own standing switch-prescriptions.html
live-only WARN).

Three injections run one at a time against the scratch copy's own
gbp-packs/riddings-timperley.md, restored by byte copy from the tracked file
(this mount's earlier passes recorded that unlink is rejected) and
sha256-reconfirmed identical to the original before the next:

1. "- Ear wax removal: microsuction ear wax removal by appointment." added
   as a new Services section bullet, directly under the Private consultation
   room line - CAUGHT. Exactly one new FAIL: "Services section lists 'Ear
   wax removal', which is not a service any pack in this repo uses... If
   the branch genuinely offers it, add it to RECOGNISED_SERVICES in
   tools/check-gbp-packs.js deliberately." Named the exact offending label,
   the same message shape the rule's own birth-pack proof produced.

2. Restored, then ", Dental clinic" appended to the Categories section's
   "Add if not present" bullet - CAUGHT. Exactly one new FAIL: "Categories
   section names 'Dental clinic', which is not a category any pack in this
   repo uses... If the branch genuinely offers it, add it to
   RECOGNISED_CATEGORIES in tools/check-gbp-packs.js deliberately."

3. Restored, then a control test: "- NOTE: the live listing may show Ear
   wax removal from an older paste; do not treat as authoritative." added
   as a new Services bullet (same unrecognised label as injection 1, but
   inside a NOTE: bullet) - PASSED, exit 0, zero mention of Riddings in the
   output beyond the standing switch-prescriptions.html WARN. Confirms the
   NOTE-bullet exemption (written so clear-aintree.md's own prose note about
   a service it does NOT offer is not misread as a claim) holds correctly on
   this pack's own copy too, not only on the pack it was written against.

All three behaved exactly as designed: the allowlist fires on a genuinely
invented label in either section, and does not fire on the same label
written as a note to the paster rather than a claim.

RESTORE AND VERIFY
Scratch copy restored to the tracked file's exact bytes after the last
injection; sha256 reconfirmed
(8cc587968d3f6b83a3509aa27151c7dc30172b626b9d0fed824630a775917c04). Full
36-checker suite re-run on the scratch copy after final restore: 35/36 exit
0, the one exception (check-cdn-pins.js) failing only because the scratch
copy has no .git directory to resolve pinned refs against, the same known
artefact of testing outside a git working tree that the item 4.13 twelfth
pass recorded - not a finding, and confirmed by the tracked repo's own
check-cdn-pins.js passing cleanly throughout (never touched). Tracked repo:
sha256 of gbp-packs/riddings-timperley.md unchanged throughout
(8cc587968d3f6b83a3509aa27151c7dc30172b626b9d0fed824630a775917c04),
git status --porcelain empty on gbp-packs/, modules/, core/, tools/,
branches.json, status/, all 36 checkers 36/36 exit 0. Scratch directory
deleted after use.

No in-repo defect found: the rule holds on Riddings' own copy exactly as it
does on the pack it was proved against at birth.

LIVE HALF
NOT performed: mcp__claude-in-chrome__tabs_context_mcp reported "Claude in
Chrome is not connected" this run (standing Q59). All live-side findings
still rest on the seventh pass's 2026-09-01 check: Post B's canonical URL
(switch-prescriptions-riddings-timperley.html) still 404, the live switch
page still sits at the old permalink switch-prescriptions.html and is still
the pre-Phase-3 paste, the branch-specific Pharmacy First page remains live
and correct, weight-loss-clinic-timperley.html's Regime 1 breach per
compliance/WEIGHT_LOSS_LIVE_PAGE_ASSESSMENT.md is unchanged and is item
5.8's territory with Rishi's Q5 answer already setting the fix direction
(GBP pack's own Post C not implicated). This pass's own subject (the
allowlist rule) governs pack wording pasted directly into a Google Business
Profile field, which is not a URL this repo can fetch, the same point made
about CLINIC_QUALIFIERS on the twelfth pass.

ANSWER PICKUP (step 3)
Also unavailable for the same reason (Chrome not connected). QUESTIONS.json
read directly: 98 total, 45 open, unchanged by pickup this run.

AUTONOMOUS WINDOW (step 4)
Grepped the top of AGENT_LOG.md for "Standing authorisation": no such
heading present. Not applicable this run.

QUESTIONS
No new question raised: pure re-verification of documented checker
behaviour against this pack's own copy, no business, legal, pricing or
regulatory content changed, no judgement call for Rishi.

Done 2026-09-06.
