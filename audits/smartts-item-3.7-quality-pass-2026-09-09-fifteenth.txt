Item 3.7 (Smartts Chemist, Bootle) - fifteenth quality pass, 2026-09-09
(unattended scheduled run, audit-backlog-worker, Cowork sandbox
mcp__workspace__bash throughout)

SELECTION
36-item rotation pool re-derived from first principles (43 checked worklist
items minus the seven standing out-of-rotation items: 1.1, 1.4, 2.2, 5.6, 5.7,
6.7, 6.8). git log -1 --format=%aI --all --grep="Item N " -i -- AGENT_WORKLIST.md
per candidate (excluding items already touched earlier on 2026-09-09: 3.11,
1.2, 4.14, 4.4, 4.1, 3.2, 3.5): 3.7 stalest at 2026-09-08T20:11:25+01:00, ahead
of 3.13 (22:11:54) and every other pool item. Chosen: 3.7 (Smartts Chemist,
Bootle).

FRESH ANGLE
Fourteen prior passes proved check-nap.js, check-postcodes.js,
check-em-dashes.js, check-contraception-copy.js, check-jsonld.js,
check-map-embeds.js, check-fragment-targets.js, check-seo-sheets.js,
check-widget-diaries.js, check-switch-copy.js (5/11 rules), check-booking-
routes.js, check-seo-pattern.js (partial), check-opening-hours.js (rule 8) and
check-branch-identity.js (rules 1/2/3/8) against Smartts by injection.
check-app-membership.js - the checker guarding the one boolean (hasApp) that
decides whether Smartts's switch page carries the "Download our app" card -
had never been named against Smartts once (confirmed: `grep -c smartts -i
tools/check-app-membership.js` returned 0 before this pass). Smartts is one of
only four hasApp:true branches in the estate and sits directly beside SK
Chemists Bootle (hasApp:false) in branches.json and in the same town - the
exact adjacent-record danger case the checker's own header names as the reason
it exists. This pass closes that gap for the rules that apply to Smartts
(rules 2, 4, 5, 6, 7; rule 3 does not apply - Smartts has no branch landing
page; rule 8/8e applies to gbp-packs/smartts-bootle.md, which is item 4.10's
own scope per the twelfth pass's scoping note, not picked up here).

BASELINE
All 35 tools/check-*.js run individually against the tracked repo before any
work: 35/35 exit 0. git status --porcelain -- gbp-packs modules core
branches.json tools status: only the two pre-existing untracked strays every
recent pass has recorded (gbp-packs/.fuse_hidden0000000400000001,
modules/service/pages/notarealservice-fishlocks-ainsdale.html.bak), not this
run's scope.

METHOD NOTE
Root filesystem (/) was at 99% capacity (165MB free), the same recurring
finding several recent passes have logged. Scratch work moved to
/sessions/eloquent-compassionate-hopper/mnt/outputs/scratch-37-appmembership
(tar --exclude='.git', 328MB free target confirmed plenty of space). Baseline
re-run on the scratch copy matched the tracked repo exactly (4 members, 21
pages in the two rendering families, 6 carrying the app, 15 packs read, 0
failures) before any injection.

INDEPENDENT EXTRACTION
audits/verify-3.7-2026-09-09-fifteenth.js, written fresh, shares no code with
tools/ or any prior pass's script. Confirms smartts_bootle.hasApp is literally
true and the adjacent SK Chemists Bootle record's hasApp is literally false;
the switch page carries exactly one app-card, one canonical app-copy sentence,
one Apple Store link and one Google Play link; all 11 non-switch Smartts
service pages carry no app mention and no store URL; Smartts has no branch
landing page; and both modules/switch/pages/INDEX.md and SEO.md mark "Smartts
Chemist - Bootle" (and only that heading, not "SK Chemists - Bootle") as an
app member. 34 checks, 0 flags.

FIVE INJECTIONS on the scratch copy, each restored from a byte-for-byte backup
and sha256-reconfirmed identical before the next:
(1) RULE 2 (switch pages) - app-card class removed from Smartts's switch page.
    CAUGHT first attempt: "is an app member in branches.json but the page
    carries no app card."
(2) RULE 4 (absence elsewhere) - the canonical app sentence appended (as an
    HTML comment, since these are fragment pages with no </body>) to
    uti-treatment-smartts-bootle.html, a non-switch Smartts page. CAUGHT first
    attempt: "mentions the app or carries a store URL, and no page in this
    family is meant to."
(3) RULE 5 (one name) - "RB Healthcare Pharmacy app" changed to "RB Healthcare
    app" on Smartts's switch page, the exact historical Fishlocks-landing-page
    fault this checker's own header records. CAUGHT first attempt: "names the
    app \"RB Healthcare app\" but the generator that owns the store buttons
    calls it \"RB Healthcare Pharmacy app\"."
(4) RULE 6 (store urls) - Smartts's Apple Store link swapped for an
    undeclared URL. CAUGHT first attempt: "carries a store URL the generator
    does not declare."
(5) RULE 7 (paste markers) - the "*(app member)*" marker slid from Smartts's
    own INDEX.md heading to SK Chemists Bootle's heading (same town, adjacent
    brand, marker COUNT unchanged at 4 so the count sub-rule alone would not
    have caught this). CAUGHT first attempt, by the brand+town identity
    sub-rule specifically: "\"SK Chemists - Bootle\" is marked as an app
    member and branches.json does not agree." Confirms by direct injection,
    for the first time against this exact branch pair, the scenario the
    checker's own source comment describes as "found by its own negative
    test."

All five caught on their intended rule only, first attempt, with no
cross-firing onto any other Smartts page, any other branch, or an unrelated
rule. Final restore sha256/cmp-reconfirmed identical to the pre-injection
backup for all three touched files (switch-prescriptions-smartts-bootle.html,
uti-treatment-smartts-bootle.html, INDEX.md).

VERIFICATION
Full 35-checker suite re-run individually on the scratch copy after the final
restore: 34/35 exit 0; check-cdn-pins.js fails only on the .git-less scratch
copy (cannot resolve pin refs without git history), the same documented
artifact several recent passes have recorded - separately re-run directly
against the tracked repo (which has .git), it exits 0 (3 warnings, 7 known
issues, all pre-existing and unrelated to this pass). Tracked repo confirmed
untouched throughout: git status --porcelain -- modules gbp-packs core
branches.json tools status showed only the two pre-existing untracked strays,
both before and after; sha256 of all three touched files on the tracked repo
matched the pre-work baseline exactly. No generator, checker, page or
branches.json content changed anywhere in the tracked tree; no regeneration
needed.

LIVE HALF NOT PERFORMED
Built-in Claude Browser MCP: request_access to smarttschemist.co.uk was
explicitly declined (unattended session, nobody present to approve).
mcp__workspace__web_fetch: refused, "URL not in provenance set". Per this
item's own eleventh-pass rule and this session's platform rules, no
curl/PowerShell workaround was attempted once both routes declined. State
stands as last recorded on the fourteenth pass (2026-09-08, no live half
performed that pass either); the thirteenth pass's own note (hours-card lunch
closure, switch page live tab title, Q16/5.8 KNOWN_CLAIM services-grid
wording, last reconfirmed 2026-08-31 sixth pass) was not re-verified this pass
and should not be assumed unchanged.

ANSWER PICKUP (step 3)
Built-in Claude Browser MCP preview_start + get_page_text on
https://data.rbhealth.co.uk/api/feedback succeeded directly, no Cloudflare
Access redirect. Newest portal entry present: "AUDIT ANSWER Q52"
(2026-09-01T22:44:51Z). QUESTIONS.json: 101 total, 48 open at run start; Q37
and Q43 both carry portal replies but neither is an actionable decision
("i need further explanation..." / "Unsure..."), correctly left open, matching
their existing QUESTIONS.json status. Nothing new to apply this run.

AUTONOMOUS WINDOW CHECK (step 4)
No "Standing authorisation - autonomous window" heading present at the top of
AGENT_LOG.md at run start. Not applicable.

RESULT
Zero in-repo defect. check-app-membership.js's rules 2, 4, 5, 6 and 7 already
correctly and independently protect Smartts Chemist Bootle's own pages and
paste sheets against exactly the adjacent-brand, adjacent-town confusion the
checker was written to prevent; now proven directly by injection for the
first time in fifteen passes. No new question. QUESTIONS.json unchanged (101
total, 48 open).

NOT YET PROVEN BY INJECTION AGAINST SMARTTS, FOR A FUTURE PASS
check-app-membership.js rule 8/8e against gbp-packs/smartts-bootle.md
(item 4.10's own scope); check-switch-copy.js's remaining 6/11 rules;
check-seo-pattern.js's service-word and H1-exact-match rules specifically;
and, per the twelfth pass's own list, the remaining previously-unnamed
checkers: check-address-region.js, check-branch-links.js,
check-brand-spelling.js, check-editor-snapshot.js, check-page-coverage.js,
check-pharmacy-first-cost.js, check-pharmacy-first-eligibility.js,
check-pharmacy-first-safety-net.js, check-pharmacy-first-symptoms.js,
check-seo-keywords.js, check-seo-lengths.js, check-service-links.js,
check-travel-clinic-copy.js, check-uk-spelling.js, check-url-scheme.js,
check-weight-loss-copy.js and check-whatsapp-route.js.
