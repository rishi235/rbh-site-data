Item 4.15 (Tiffenbergs Chemist Aintree GBP pack) - twentieth quality pass
Run 106, unattended scheduled run, audit-backlog-worker, 2026-09-18

FRESH ANGLE
-----------
tools/check-postcodes.js has never been proven by direct injection against
gbp-packs/tiffenbergs-aintree.md across nineteen prior passes on this item,
despite being proven this run cycle against sibling packs (clear-aintree.md,
item 4.9 fourteenth pass; fishlocks-eccleston.md, item 4.8 twenty-first pass;
mccanns-sandringham.md, item 1.3 quality pass, multiple times). Grep-confirmed
by searching this item's own full text for "check-postcodes" and "postcode":
the only hit was an incidental note about scratch-file baseline pollution on
the tenth pass, not an injection record.

BASELINE
--------
Pack sha256: 59d288c1c32920c05bc9b12479ec2bc8970ffa5639977ccee10ec9be9046811b
(matching all nineteen prior passes, no drift).
Full 35-checker suite (36 minus check-cdn-pins.js/check-live-hours.js, both
network/git-dependent) run individually against the tracked repo before any
work: 35/35 exit 0.

METHOD
------
Full repo copied via `git archive HEAD | tar -x` to a disposable /tmp scratch
directory. Tracked repo never opened for writing during the injection phase.
Each injection restored from a saved byte copy of the original pack and
sha256-reconfirmed identical before the next round.

INJECTIONS
----------
1. RULE 1 (UNKNOWN). Post D (travel clinic) body extended with "We also see
   travellers coming from as far as L9 9DC." - a one-character-off variant of
   this branch's own L9 9DB, matching no branch and not in NARRATIVE_POSTCODES.
   Result: CAUGHT, exit 1, exactly one failure:
     UNKNOWN  gbp-packs/tiffenbergs-aintree.md: postcode L9 9DC is in no
     branches.json entry
   First attempt, exactly the intended rule.

2. RULE 3 (FOREIGN). Photo shot list's entrance bullet extended with "Free
   on-street parking bay directly outside, postcode L20 5DW for satnav if
   approaching by car." - SK Chemists Bootle's real postcode, on a line naming
   no branch, so FOREIGN could be isolated from MISATTRIB.
   Result: CAUGHT, exit 1, exactly one failure:
     FOREIGN  gbp-packs/tiffenbergs-aintree.md: owned by tiffenbergs_longmoor
     (L9 9DB) but carries L20 5DW (skchemists_bootle)
   First attempt, exactly the intended rule, no MISATTRIB co-fire (line names
   no branch).

3. RULE 6 (MISATTRIB). Services section extended with "Unlike McCanns Chemist
   Aigburth (L9 9DB), this branch also runs a dedicated travel clinic diary."
   - names McCanns Chemist Aigburth but carries Tiffenbergs' OWN postcode
   (L9 9DB), not McCanns' real L17 7BP. Since L9 9DB is this file's own
   branch's postcode, FOREIGN does not apply; only the branch-name/postcode
   mismatch on the line should fire.
   Result: CAUGHT, exit 1, exactly one failure:
     MISATTRIB gbp-packs/tiffenbergs-aintree.md:51: line names McCanns Chemist
     Aigburth (L17 7BP) but carries L9 9DB (tiffenbergs_longmoor)
   First attempt, exactly the intended rule, no FOREIGN co-fire. Confirms the
   MISATTRIB_KNOWN exemption (keyed to the one pre-registered Hirshmans/
   Tiffenbergs comparative sentence in
   compliance/WEIGHT_LOSS_LIVE_PAGE_ASSESSMENT.md) does not blanket-cover
   every "Unlike X" phrasing - an unregistered comparative sentence naming a
   different branch still fails correctly.

4. CONTROL. Services section extended with "Tiffenbergs Chemist's own
   registered postcode is L9 9DB, confirmed correct against branches.json." -
   own brand name, own correct postcode.
   Result: PASSED, exit 0, same 3 pre-existing UNOWNED warnings as baseline,
   45 distinct postcodes, 16 live branches, 0 failures. Confirms no spurious
   firing on a legitimate self-referential sentence.

All four rounds behaved exactly as designed on the first attempt, each on its
intended rule only, no side-effect failures anywhere else in the suite.

VERIFICATION
------------
Pack restored to the original byte-for-byte after each round and after the
final round; sha256 reconfirmed 59d288c1c32920c05bc9b12479ec2bc8970ffa5639977
ccee10ec9be9046811b, matching baseline throughout. Full 35-checker suite
re-run clean on the scratch copy after the final restore (35/35 exit 0).
Tracked repo's own copy of the pack confirmed byte-identical throughout
(sha256 unchanged); `git status --porcelain -- gbp-packs tools modules core
branches.json` showed only the two long-standing untracked strays every
recent entry has recorded and left alone
(gbp-packs/.fuse_hidden0000000400000001,
modules/service/pages/notarealservice-fishlocks-ainsdale.html.bak). No
checker logic edited, no pack content byte changed.

RESULT
------
No in-repo defect found. check-postcodes.js's rules 1 (UNKNOWN), 3 (FOREIGN)
and 6 (MISATTRIB) now all proven by direct injection against this specific
pack for the first time in twenty passes. Rules 0 (MALFORMED), 2 (MISSING),
4 (DISPOSED) and 7 (DUPLICATE) remain unproven against this pack specifically
and are recorded as a candidate fresh angle for a future pass (rule 2 is
already satisfied passively, since the branch's postcode does appear outside
a narrative file; rule 4 has no natural target here since Tiffenbergs is a
live, not disposed, branch; rule 7 tests branches.json against itself rather
than this pack).

LIVE HALF
---------
Not attempted this pass. Claude in Chrome tools were used earlier in this
run's own step 3 answer pickup (see AGENT_LOG.md); this pass's own scope is a
repo-internal injection proof and check-postcodes.js's rules govern text
typed into GBP's picker fields, not a URL a fetch could verify regardless.
The seventeenth pass's live verdicts (2026-09-15) stand, not re-verified this
pass.

No new question raised. QUESTIONS.json unchanged at 111 total, 58 open
(confirmed at both the start and end of this run, not opened for writing).
