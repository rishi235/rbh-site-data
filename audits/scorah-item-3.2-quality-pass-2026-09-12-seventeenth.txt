Item 3.2 (Scorah Chemists, Bramhall and Hazel Grove) - quality pass, seventeenth
Unattended scheduled run, audit-backlog-worker, eighth run today (2026-09-12)

SELECTION
All eight remaining [ ] worklist lines confirmed still [BLOCKED] (5.3, 5.4, 5.5,
5.8, 6.1, and the three Q60/Q66 lines under 6.4/6.5/6.6), so the quality-pass
fallback applies. Rotation pool independently re-derived by a Python scan of
AGENT_WORKLIST.md: for every "- [x] N.M" header, the block of text up to the
next header was searched for "2026-09-12" co-occurring with "quality pass".
Items touched today before this run: 1.2, 1.3, 2.1, 2.3, 3.1, 6.2, 6.3. Eligible
pool is the standing 36-item set minus the standing out-of-rotation set (1.1,
1.4, 2.2, 5.6, 5.7, 6.7, 6.8) minus those seven. Lowest-numbered remaining
candidate: 3.2, matching the seventh run's (item 3.1's) own forward note
exactly.

WHY THIS ANGLE
Sixteen prior quality passes on item 3.2 have proven, by direct injection
against Scorah's own 26 pages or its own two GBP packs: check-seo-pattern.js
(title/H1/description town and service words, cross-town absence, H1 count,
duplicate title/description lines), check-seo-lengths.js, check-seo-sheets.js,
check-seo-keywords.js, check-branch-identity.js (data-branch and JSON-LD name),
check-fragment-targets.js, check-jsonld.js, check-opening-hours.js,
check-branch-links.js, check-booking-routes.js and check-gbp-packs.js (the
website-vs-shared-homepage and catchment-order rules). A text search of the
item's own history for check-app-membership.js returned zero hits - it had
never been named once for item 3.2, despite both Scorah branches carrying
hasApp: false in branches.json and both having generated switch pages, a
landing page and a GBP pack, all three of the page/pack families this checker
reads.

BASELINE
Full 36-checker suite run individually against the tracked repo before any
work: 35 clean, one pre-existing failure in tools/check-postcodes.js (postcode
"L4 9SG" flagged as UNKNOWN in AGENT_LOG.md, AGENT_WORKLIST.md and two audit
files with no NARRATIVE_POSTCODES entry). This is the same residue the
seventh run today (item 3.1's pass) recorded and explicitly left unfixed as
out of that item's scope; it is equally out of this item's scope (nothing
about the postcode injection concerns Scorah or check-app-membership.js) and
is left unfixed here too, for whoever next runs a general-hygiene or item 1.3
pass. branches.json sha256 recorded as
904de09bc3118cefcfd7ae3f8e045b9ea1d090c634c70114f135101f0b969e1e, matching
the standing regression anchor throughout this pass.

check-app-membership.js baseline: clean, 4 app members of 16 branches
(fishlocks_ainsdale, fishlocks_eccleston, clearchemist_aintree,
smartts_bootle - neither Scorah branch), 21 pages checked across the switch
and landing families, 15 GBP packs read.

METHOD
Full repo copied by tar (excluding .git) to a scratch directory under the
outputs mount. Tracked repo never opened for writing. Six injections in total,
one per distinct rule leg of check-app-membership.js not yet exercised against
a Scorah page or pack, each restored by direct copy from the tracked repo and
sha256-reconfirmed byte-identical before the next injection:

1. RULE 2 (switch pages, false-positive direction): the full "app-card" block
   (head, copy line naming the app, two store buttons) copied verbatim from
   Fishlocks Ainsdale's switch page onto
   modules/switch/pages/switch-prescriptions-scorah-bramhall.html (hasApp
   false). CAUGHT: "carries the app card but branches.json says this branch
   is not an app member." Exactly one failure. Restored; sha256
   427c506d76e0fb12cfdef9866c7afd2b2148b5095d822c27fff7135714ca843d confirmed
   before and after.

2. RULE 3 (landing pages, false-positive direction): the "Manage everything in
   the free RB Healthcare Pharmacy app" sentence appended to the repeat-
   prescriptions line on modules/branch/pages/pharmacy-scorah-hazel-grove.html
   (hasApp false). CAUGHT: "carries the app sentence but branches.json says
   this branch is not an app member." Exactly one failure. Restored; sha256
   89159a1b1c7a60e6c4f5da457435a6feb25de374b61962cf6940b44fa67e5c0d confirmed
   before and after.

3. RULE 4 (absence elsewhere): a stray app-mention paragraph appended to
   modules/service/pages/contraception-scorah-bramhall.html, a family (service
   pages) the rule says should never carry the app at all regardless of
   hasApp. CAUGHT: "mentions the app or carries a store URL, and no page in
   this family is meant to." Exactly one failure. Restored; sha256
   ec1b6a1eb19b4fe972e2072c72b30625f40ee9f2ee613bfb61c2576f615f4044 confirmed
   before and after.

4. RULE 8b (GBP pack photo shot list): an app-related shot line ("Customer
   using the RB Healthcare Pharmacy app on their phone at the counter.") added
   to gbp-packs/scorah-bramhall.md's photo shot list (hasApp false). CAUGHT:
   "the photo shot list asks for an app shot, but branches.json has hasApp
   false for scorah_bramhall." Exactly one failure. Restored; sha256
   e592ab24164aeb093cd9b1bd58a2ee52ac809e01bd822678dafe95843ad1bb2c confirmed
   before and after.

5. RULE 8c (GBP pack paster note vs field): gbp-packs/scorah-hazel-grove.md's
   "No app mention anywhere in this pack: branches.json has hasApp false for
   this branch" note rewritten to state hasApp true instead (hasApp is
   actually false). CAUGHT: "the paster note tells the paster branches.json
   has hasApp true, but it has false for scorah_hazel, so the note invites an
   app claim onto a profile that must not carry one." Exactly one failure.
   Restored; sha256
   64fd2ae61c8ae0ca7ac5565a9f6cc2ad4485869a465b98a691093c8b2b7a4038 confirmed
   before and after.

6. RULE 8d (GBP pack note vs published copy, deliberately allowed to co-fire
   with 8a): a sentence ("You can also manage repeat prescriptions through our
   app.") appended to the business description of
   gbp-packs/scorah-hazel-grove.md, whose paster note already states "No app
   mention anywhere in this pack". CAUGHT BOTH: rule 8a ("the copy pasted into
   the public Google profile claims an app, but branches.json has hasApp
   false for scorah_hazel") and rule 8d ("the paster note says there is no
   app mention anywhere in this pack, but the pasted copy carries one") fired
   together, exactly as the checker's own design intends (8a and 8d are not
   mutually exclusive; 8a reads the field, 8d reads the note against the pack
   it describes). Two failures, both expected, no unexplained third. Restored;
   sha256 64fd2ae61c8ae0ca7ac5565a9f6cc2ad4485869a465b98a691093c8b2b7a4038
   confirmed before and after (same file as test 5, re-confirmed clean of
   test 5's edit before test 6 began).

Rule 1 (field present) is trivially true for both Scorah branches (hasApp:
false, a real boolean) and not worth an injection against branches.json's own
schema, already covered generically by every other item's passes. Rules 5 and
6 (one app name, store URLs) depend on the two generators' own declared
strings, not on any branch-specific data, and were judged out of scope for a
branch-specific pass; they have no realistic Scorah-specific failure mode
distinct from any other branch's. Rule 7 (paste marker count and brand/town
matching) is estate-wide by construction (it counts markers across the whole
switch INDEX.md/SEO.md against the whole of branches.json) rather than
Scorah-specific, and was also left for a future estate-wide pass. Rule 8e (the
missing positive - a member's pack must actually carry the app mention) does
not apply to either Scorah branch, since neither is a member; it is already
proven against Smartts Bootle/Fishlocks/Clear Chemist packs per the checker's
own header comment, dated the item 4.1 quality pass, 2026-09-04.

VERIFICATION AFTER RESTORATION
Full 36-checker suite (35 checkers plus check-cdn-pins.js, the latter excluded
from the scratch-copy run per established convention since the scratch copy
has no .git and check-cdn-pins.js always fails there on "ref does not resolve
in git") re-run on the scratch copy after all six restores: clean except the
same pre-existing check-postcodes.js finding recorded in the baseline (matched
line for line). Tracked repo confirmed untouched throughout:
`git status --porcelain -- modules core branches.json gbp-packs tools` showed
only the two pre-existing untracked strays
(gbp-packs/.fuse_hidden0000000400000001,
modules/service/pages/notarealservice-fishlocks-ainsdale.html.bak), neither
created or touched by this pass. branches.json sha256 unchanged at
904de09bc3118cefcfd7ae3f8e045b9ea1d090c634c70114f135101f0b969e1e. All six
target files' sha256 re-confirmed identical to their pre-injection baseline on
the tracked repo. check-app-membership.js run directly against the tracked
repo one final time: clean, identical output to the baseline run.

LIVE HALF
Not performed this pass: Claude in Chrome reported "not connected" when
checked before step 3's answer-pickup attempt, per standing Q59, and no
alternative browser or fetch route was substituted (per this run's own
procedure, no retry and no alternative route on a Chrome failure). The
previously logged live-only findings for Scorah (both branch landing pages
404; six sampled live pages declaring the bare shared brandLabel "Scorah
Chemists" rather than the branch-specific JSON-LD name, first found on the
2026-09-06 twelfth pass) were not re-checked this pass and should not be
assumed unchanged.

RESULT
No in-repo defect found. check-app-membership.js's rules 2, 3, 4, 8b, 8c and
8d all proven to bite correctly, first attempt, on their own intended rule
only, when exercised specifically against Scorah's own pages and packs for the
first time in this item's seventeen-pass history. No checker logic, generator,
branch data, page or pack content changed anywhere in the tracked repo. No new
question raised on item 3.2 itself.

FORWARD NOTE
Checkers now proven against Scorah's own data across seventeen passes:
check-seo-pattern.js, check-seo-lengths.js, check-seo-sheets.js,
check-seo-keywords.js, check-branch-identity.js, check-fragment-targets.js,
check-jsonld.js, check-opening-hours.js, check-branch-links.js,
check-booking-routes.js, check-gbp-packs.js and (this pass)
check-app-membership.js, plus collateral catches on check-nap.js,
check-postcodes.js and check-map-embeds.js. Checkers still never named once in
this item's own history: check-whatsapp-route.js, check-brand-spelling.js,
check-em-dashes.js, check-address-region.js, check-service-links.js,
check-page-coverage.js's ORPHAN_PAGE rule specifically, check-editor-snapshot.js
as the primary target rather than collateral, and check-live-hours.js as the
primary target. Neither Scorah branch runs weight loss/travel clinic/
contraception copy that differs from the estate norm in a way that would make
check-weight-loss-copy.js, check-travel-clinic-copy.js or
check-pharmacy-first-eligibility.js/-safety-net.js/-symptoms.js/-cost.js
Scorah-specific targets; they remain estate-wide checkers already proven
elsewhere. check-switch-copy.js is a strong candidate for the next pass on
this item: it has ten rules, has never been named for item 3.2, and Scorah's
switch pages are a genuine shared-domain pair worth testing (e.g. whether the
FAQ/hero claim-consistency KNOWN entries against Q49 apply identically to
both branches).
