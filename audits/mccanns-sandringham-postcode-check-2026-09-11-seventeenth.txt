Item 1.3 quality pass (seventeenth), 2026-09-11 (unattended scheduled run,
audit-backlog-worker)

SUMMARY
A real, previously-latent gap found and fixed in tools/check-postcodes.js.
No in-repo data defect, no live branch affected. Rule 6 (MISATTRIB)'s
branch-name detection was case-sensitive; every rule that reads a POSTCODE in
this file has been made case-insensitive over the sixteen prior passes, but
the branch-NAME half of the same rule never was.

ROTATION POOL DERIVATION
43 [x] AGENT_WORKLIST.md items, minus 7 standing out-of-rotation items (1.1,
1.4, 2.2, 5.6, 5.7, 6.7, 6.8), minus 4 items touched earlier today per
`git log --since="2026-09-11T00:00:00" -- AGENT_WORKLIST.md` (3.1 03:15:12+01:00,
4.3 03:40:20+01:00, 4.6 04:10:13+01:00, 4.5 04:42:59+01:00) = 32 candidates.
`git log -1 --format=%aI --all --grep="Item N.N " -- AGENT_WORKLIST.md` run per
candidate: 1.3 uniquely stalest at 2026-09-09T09:12:46+01:00, ahead of 4.13
(09:40:25), confirming the sixteenth pass's own forward note exactly. All 8
unchecked AGENT_WORKLIST.md lines reconfirmed [BLOCKED] first (5.3, 5.4, 5.5,
5.8, 6.1, 6.4, 6.5, 6.6).

BASELINE
node tools/check-postcodes.js (tracked repo, before any change):
974 text files scanned, 34 distinct postcode(s), 16 live branches:
0 failure(s), 3 warning(s) (the three standing UNOWNED notices for
gbp-packs/TEMPLATE.md and the two shared branch-family sheets,
modules/branch/pages/INDEX.md and SEO.md).

FRESH ANGLE
Sixteen prior passes on this item all widened or re-proved the POSTCODE half
of extraction (PC_RE, PC_RE_LOOSE, the &nbsp; forms in all their cases, the
URL escape forms, the file-scanning extension gap). None had tested the
BRANCH-NAME half of rule 6 (MISATTRIB), which decides which single branch a
line is "about" before checking whether the postcode on that line matches.
That half uses plain `line.indexOf(b.branchName)` and
`line.indexOf(aliasOf(b))`, both case-sensitive.

PROOF (scratch copy, /tmp/pcscratch17, rsync of the full tracked repo,
outside the working tree)
Baseline on the scratch copy matched the tracked repo exactly: 974 files
scanned, 0 failures, 3 warnings.

Test 1 (contaminated, discarded): appended a line to
compliance/WEIGHT_LOSS_LIVE_PAGE_ASSESSMENT.md naming "Smartts Chemist
Bootle's" (properly cased) alongside a wrong postcode. This was caught,
because the properly-cased brand name still matched case-sensitively - not a
useful test of the case-sensitivity gap. Discarded, file restored to the
pre-injection sha256
(0525909b0a566d99ab25e6355b4a55d2a9c188b6531de9d34f3f787295e8a03d) and
reconfirmed.

Test 2 (isolated the gap): appended, to the same file:
  "riddings timperley's registered address postcode is l20 5dw, not this
   branch's own wa15 6bp."
Riddings Pharmacy Timperley's real postcode is WA15 6BP; L20 5DW is SK
Chemists Bootle's real postcode (confirmed via branches.json lookup) - a
genuine MISATTRIB shape, purely lower case, no properly-cased brand word
anywhere on the line. Unmodified checker on the scratch copy: 974 files
scanned, 0 failures, 3 warnings - identical to baseline, the line invisible
to rule 6 entirely (named.length came out 0, not even reported ambiguous).
File restored, sha256 reconfirmed identical to baseline.

REPO-WIDE BEFORE/AFTER CHECK (required before applying, same convention as
the twelfth pass's alias-widening proof)
Wrote a standalone comparison script (not committed) that walks every
in-scope file (excluding NARRATIVE_FILES, audits/, and DECLARING) and, for
every line, compares the set of branches matched under the OLD
(case-sensitive) rule against the NEW (case-insensitive) rule.
Result: 74 lines moved from zero matches to exactly one. Every one of them
is the ALL-CAPS build-comment header line this repo's own six generators
write at the top of every generated page and every PASTE_PACK file, for
example "FISHLOCKS CHEMIST AINSDALE - branch landing page (shared-domain
split, item 2.2)." and "CHERRY LANE PHARMACY -- Weight Loss Clinic (private,
paid service -- NOT NHS)." None of the 74 lines carries a postcode today, so
the widening is a correctness improvement (these are genuine single-branch
lines the old case-sensitive match was skipping entirely, not a new source
of ambiguity) and not a coverage loss. Two further lines (in
tools/check-brand-spelling.js and tools/check-nap.js) are code comments
quoting lower-case test strings ("smartts chemist", "sk chemists"); neither
carries a postcode.

Note: this comparison, run against the full repo tree, also surfaced an
untracked stray directory literally named "C:" at the repo root, containing
what look like OneDrive PASTE_PACK files from a past Windows-MCP path
confusion (the sandbox FUSE quirk under Q87/Q96). It is untracked
(`git status --porcelain -- "C:"` shows `?? C:/`), carries no postcode
mismatch today (0 failures either side of this fix), and is out of scope for
this item; left untouched. Flagged here for whoever next does repo hygiene,
not raised as a question since it changes nothing this item is responsible
for.

FIX
tools/check-postcodes.js, rule 6 block (inside checkFile()): the line and
every candidate branchName/alias are now compared in lower case for MATCHING
purposes only. The failure message still names the branch via its real-cased
b.branchName field, unchanged. Full rationale and proof recorded in a comment
directly above the changed block.

RE-VERIFICATION (scratch copy)
1. Fixed checker, no injection: 974 files, 0 failures, 3 warnings - identical
   to baseline, confirming the 74 newly-visible header lines introduce no
   false positive.
2. Fixed checker, Test 2 injection re-applied: FAILED correctly -
   "MISATTRIB compliance/WEIGHT_LOSS_LIVE_PAGE_ASSESSMENT.md:636: line names
   Riddings Pharmacy (WA15 6BP) but carries L20 5DW (skchemists_bootle)" -
   1 failure, 3 warnings (unchanged). Only the wrong postcode is flagged; the
   correct WA15 6BP on the same line is correctly excluded since it equals
   `want`.
3. Restored by direct byte edit (removing the appended lines) and
   sha256-reconfirmed identical to the pre-injection value both times.
4. Full re-run after restore: 974 files, 0 failures, 3 warnings.

APPLIED TO TRACKED REPO
tools/check-postcodes.js patched with the proven fix and comment.
node -c tools/check-postcodes.js: syntax OK.
node tools/check-postcodes.js: 974 files scanned, 34 distinct postcodes, 16
live branches, 0 failures, 3 warnings - unchanged from the pre-fix baseline.
All 36 tools/check-*.js checkers run individually: all exit 0.
git status --porcelain -- modules/ core/ branches.json: empty (only
tools/check-postcodes.js shows as modified in the full git status). No
generator, page, pack, paste block or branches.json field touched; no
rebuild needed (check-postcodes.js is not a generator input, the same
convention recorded for compliance/ and gbp-packs/ in earlier passes).

LIVE HALF
Network reachable this run. https://www.mccannspharmacy.co.uk/pharmacy-first-service-aigburth.html
returned HTTP 200 and carries both correct postcodes (L17 7BP for Aigburth,
L17 4JP for Sandringham); no CH49 1SX anywhere on the page. Consistent with
every prior pass.

QUESTIONS
None raised. This is a mechanical case-sensitivity fix to a checker's own
name-matching logic, proved safe estate-wide before being applied, the same
class of fix as fifteen of the sixteen prior passes on this item.

Evidence: this file. Scratch copy discarded after use
(/tmp/pcscratch17, not part of the repo).
