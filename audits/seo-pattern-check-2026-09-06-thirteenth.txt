Item 3.1 quality pass (thirteenth), 2026-09-06, unattended scheduled run via Cowork.

SCOPE: tools/check-seo-pattern.js, twelve prior passes deep (2026-08-04 through
2026-09-05). Read the full 819-line checker end to end against the twelve prior
passes' own accounts (readConditions()/CONDITIONS fail() path, untyped-file-fails,
KNOWN_NON_PAGE three-state proof, PAGE_TYPES forward call-check and its reverse
half, stale-KNOWN_NON_PAGE_BUILDER-key path, town/brand data-source rule, region
leg and its vacuity guard, checkMeta's length-bound rule, H1_OPEN_RE vs the bare
<h1> exact-match distinction, KNOWN_SISTER_TOWN's stale-key rule) to find a genuinely
untested angle rather than repeat one.

FOUND: the PAGE_TYPES forward contract check has five failure branches (lines
626-670 of tools/check-seo-pattern.js): missing builder file, builder that does
not require ./seo-pattern, title/h1 expression naming no function (fnMatch fails),
named function not exported by seo-pattern.js, and builder that never calls its
named function. The original 2026-08-13 construction pass (AGENT_LOG.md, third
pass) recorded "negative-tested eight ways": missing builder, function not
exported, dropped require, named function never called, emptied PAGE_TYPES, new
generator outside the contract, and a stale KNOWN_NON_PAGE_BUILDER key. The
"title or h1 expression naming no function" sub-branch (fnMatch returning null on
a value with no call syntax, e.g. a bare identifier or empty string) was described
in the fix's own prose ("a title or h1 expression naming no function fails") but
was not in the negative-tested list and no AGENT_LOG.md/AGENT_WORKLIST.md/audits
entry across all thirteen passes shows it exercised by injection. Confirmed absent
by grep across AGENT_LOG.md, AGENT_WORKLIST.md, QUESTIONS.json and audits/*.txt
for "which names no" / "names no function": zero hits before this pass.

BASELINE (tracked repo, before any scratch work): self-test passed with no length
warnings. All 36 tools/check-*.js run individually, 36/36 exit 0. All six page
generators (build-branch-landing-pages.js, build-contraception-pages.js,
build-service-pages.js, build-switch-pages.js, build-travel-clinic-pages.js,
build-weight-loss-pages.js; build-audit-status.js excluded per KNOWN_NON_PAGE_BUILDER
convention) rebuilt from branches.json: sha256 of the combined sorted file list
under modules/ and core/ (*.html, *.js, *.css, *.md) taken before and after,
identical both times (a051e64de6ed9aca89fd6b428b37d5df3424056bfc5a4170d846aa0c5c4daad7).
git status --porcelain -- modules core branches.json tools gbp-packs status empty
throughout. check-seo-pattern.js itself: 177 pages, 0 untyped, 0 failures, the
same two PINNED cross-town cases as every recent pass (Q71, still open, not a
failure: McCanns Sandringham naming Aigburth, Scorah Bramhall and Hazel Grove each
naming the other).

METHOD: git archive HEAD | tar -x into /tmp/scratch-3.1-13th, so the tracked
working tree was never opened for writing during the injection round. Scratch
copy's own baseline run confirmed byte-identical output to the tracked repo
(177/0/0) and tools/seo-pattern.js sha256-matched the tracked HEAD copy before
any mutation.

INJECTION ROUND, two cases against the switch entry's own title field in the
scratch copy's tools/seo-pattern.js (family:"switch", the smallest of the seven
PAGE_TYPES entries so the blast radius of a mistake was easiest to reason about),
each restored by writing the tracked HEAD's own git show output back and
sha256-reconfirming byte-identical before the next case:

(1) title changed from "switchTitle(b)" to "switchTitle" (call syntax removed,
    leaving a bare identifier with no parens) - CAUGHT, exit 1, exact message
    "FAIL PAGE_TYPES 'switch' title is 'switchTitle', which names no function.
    The contract must name the pattern function it promises." Contract line
    correctly dropped from 14 to 13 title/H1 legs verified.

(2) title changed to "" (empty string, exercising the String(pair[1] || "")
    fallback on the falsy branch of the same guard, a different code path
    through the same fnMatch check) - CAUGHT, exit 1, exact message "FAIL
    PAGE_TYPES 'switch' title is '', which names no function. The contract
    must name the pattern function it promises."

Both caught on the first attempt with the expected, rule-specific message, no
cross-firing on any other brand or generator. Scratch tools/seo-pattern.js
sha256-reconfirmed identical to the tracked HEAD copy after each restore.
Scratch directory deleted entirely after use.

RESULT: zero in-repo defect. The one previously-undemonstrated sub-branch of the
PAGE_TYPES forward contract check (a malformed title/h1 expression naming no
function) is now proven by injection in both its non-empty and empty-string
forms, completing every distinct failure branch of that rule's forward half
across this item's thirteen-pass history. No checker logic, generator, page or
branches.json content changed in the tracked tree at any point; re-confirmed by
a second full 36-checker run and sha256 comparison after the scratch directory
was deleted (identical to the pre-pass baseline).

LIVE HALF: Claude in Chrome reported not connected (mcp__claude-in-chrome__navigate
to https://data.rbhealth.co.uk/api/feedback at answer pickup) - standing Q59,
logged and not retried by another route, nothing clicked, typed or submitted.
Fell back to the established read-only curl GET route from the Cowork sandbox
(network egress confirmed first via a 200 from google.com):
https://www.fishlockpharmacy.co.uk/pharmacy-first-fishlocks-ainsdale.html
returned HTTP 200, SEO title comment "Pharmacy First at Fishlocks Chemist,
Ainsdale" and H1 "Pharmacy First at Fishlocks Chemist in Ainsdale", both the
pattern verbatim, unchanged from every prior pass's live sample.
https://www.mccannspharmacy.co.uk/pharmacy-mccanns-sandringham.html returned
HTTP 404, consistent with Q35's landing-page paste backlog still being
outstanding, unchanged from every prior pass. No new live finding.

No new question raised.
