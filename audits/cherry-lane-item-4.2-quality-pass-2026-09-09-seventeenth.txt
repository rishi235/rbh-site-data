Item 4.2 (Cherry Lane Pharmacy Walton GBP pack) - quality pass 2026-09-09, seventeenth
pass, unattended scheduled run (audit-backlog-worker, Cowork sandbox mcp__workspace__bash
throughout).

FRESH ANGLE: tools/check-gbp-packs.js's "claims outside the known vocabulary" allowlist
rule (RECOGNISED_SERVICES / RECOGNISED_CATEGORIES, added item 4.4 quality pass,
2026-08-13) had never been pointed at gbp-packs/cherry-lane-walton.md specifically in
sixteen prior passes on this item. A full-text search of every prior pass's own
paragraph (grep -in "allowlist|RECOGNISED_SERVICES|RECOGNISED_CATEGORIES|not a service
any pack|not a category any pack|outside the known vocabulary") returned zero hits
against this item's own AGENT_WORKLIST.md section and its own audits/ files. The rule's
own header comment records it was originally proved against gbp-packs/scorah-bramhall.md
only; Cherry Lane was never named.

BASELINE: sha256 of gbp-packs/cherry-lane-walton.md confirmed
831e72c18ef9007d7fd760e9afcd1ce60513ac99a42bdf1ff4e014fcef052ec2, matching every prior
pass's own recorded value - no drift since the tenth pass's Q40 rename edit. Full repo
byte-copied (cp -a, .git included) to /tmp/scratch-42-allowlist, a scratch directory
outside the tracked tree; tracked repo file never opened for writing during the
injection phase. Full 34-checker suite (check-cdn-pins.js and check-live-hours.js
excluded, standing convention - the former needs live git ref resolution, the latter
needs a live ATL session) run clean on the scratch copy before any edit.

THREE CASES against the rule's two independent detection paths (categories, services)
plus a control, each restored from the saved baseline copy and sha256-reconfirmed
identical before the next:

1. CATEGORIES / FAIL CASE. "Dental clinic" appended to the "## 2. Categories" section's
   "Add if not present" bullet, alongside the three genuine categories already there
   (Travel clinic, Weight loss service, Vaccination centre) - CAUGHT first attempt:
   'Categories section names "Dental clinic", which is not a category any pack in this
   repo uses.' No RBH branch runs a dental service; this is the same shape of false
   claim the rule's own header names as proved against Scorah Bramhall, now proved
   independently against Cherry Lane.

2. SERVICES / FAIL CASE. A new bullet, "- Ear wax removal: microsuction ear wax removal
   by appointment.", appended to the end of the "## 3. Services section content"
   Private services list - CAUGHT first attempt: 'Services section lists "Ear wax
   removal", which is not a service any pack in this repo uses.' Same wording the
   rule's header cites from the original Scorah Bramhall proof, confirming the
   allowlist (not just the blocklist rules above it) is independently live against
   this pack's own copy.

3. CONTROL / PASS CASE. A new bullet, "- Blood tests: same-day finger-prick blood
   tests, results within 48 hours.", appended after the existing "Blood testing"
   bullet - a genuinely RECOGNISED_SERVICES label ("blood tests", distinct from the
   pack's existing "blood testing" entry) worded differently from anything already in
   the pack. Correctly PASSED: exit 0, no FAIL line for cherry-lane-walton.md (only the
   pre-existing Q72 known-exception WARN, unchanged). This proves the rule matches on
   the recognised label itself, not on memorised exact wording already present in the
   file - a distinction no prior pass on any pack had exercised for this specific rule.

All three cases behaved exactly as designed on the first attempt, each on its own
detection path and no other (no unrelated FAIL lines appeared in any of the three
runs). No checker logic edited, no pack content byte changed at any point.

RESTORE AND RE-VERIFICATION: pack restored to the saved baseline copy after each of the
three cases; sha256 reconfirmed 831e72c18ef9007d7fd760e9afcd1ce60513ac99a42bdf1ff4e014f
cef052ec2 (unchanged) after the final restore. Full 34-checker suite re-run clean on
the scratch copy (0 failures). Tracked repo's own copy of the pack confirmed
sha256-identical throughout and never opened for writing; `git status --porcelain --
gbp-packs/cherry-lane-walton.md tools/check-gbp-packs.js` on the tracked repo returned
empty both before and after this pass. No generator writes gbp-packs/, so nothing to
regenerate; the pack itself was never modified on the tracked repo.

RESULT: zero in-repo defect. The allowlist rule already correctly protects Cherry
Lane's own pack on both the categories and services detection paths, discriminates a
genuinely unrecognised label from a recognised one worded differently, and is now
proven directly against this pack for the first time in seventeen passes.

LIVE HALF: access to https://data.rbhealth.co.uk was granted this run (request_access,
scope "once") for the QUESTIONS.json answer-pickup step, but it redirected to
https://rbhealth.cloudflareaccess.com (Cloudflare Access login), which the task's own
rules say to leave alone rather than request access to or log into - consistent with
every recent run hitting this same Cloudflare-gated domain. Not performed for the
Cherry Lane live site itself this pass: this specific rule governs plain-text copy
pasted into a Google Business Profile picker/services field during manual paste, not
something a read-only fetch of the live website could verify regardless, matching the
same reasoning every other GBP-pack-copy quality pass in this item's own history has
used. No new live finding this pass; the sixteenth pass's live findings (Q95
cross-reference, Q92, Q36 instability) stand, not re-verified.

QUESTIONS: no new question raised this pass. QUESTIONS.json unchanged at 100 total,
47 open at pass start. No credential encountered in any file touched.
